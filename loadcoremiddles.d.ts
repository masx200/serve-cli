import { DefaultContext, DefaultState } from "koa";
export declare function loadcoremiddles(
    app: import("koa")<DefaultState, DefaultContext>,
    publicpath: string,
    index?: boolean
): void;
